---
description: 
globs: 
alwaysApply: true
---

# Cursor Rules - 项目编码规范

## 项目架构

项目遵循NestJS的模块化架构，主要结构如下：
src/
├── modules/ # 功能模块，每个模块都是独立的业务单元
│ ├── article/ # 文章模块
│ ├── user/ # 用户模块
│ ├── tags/ # 标签模块
│ └── ai/ # AI服务模块
├── common/ # 通用功能
│ ├── decorator/ # 装饰器
│ ├── dto/ # 通用DTO
│ └── entity/ # 通用实体类
├── utils/ # 工具函数
└── config/ # 配置文件


## 代码规范

### 命名规范

1. **文件命名**:
   - 使用小写字母加连字符
   - 按功能命名: `article.service.ts`, `article.controller.ts`, `article.entity.ts`

2. **类命名**:
   - 使用PascalCase: `ArticleService`, `UserController`
   - 按功能添加后缀: `Service`, `Controller`, `Entity`, `DTO`

3. **变量/方法命名**:
   - 使用camelCase: `findById`, `createArticle`
   - 方法名应表达其功能: `updateArticleApprovalStatus`

4. **常量命名**:
   - 使用大写下划线: `ARTICLE_APPROVAL_STATUS`, `CACHE_CONSTANTS`

### 模块组织

1. **模块结构**:
   - 每个功能模块包含: module, controller, service, entity, dto, constant
   - 保持一致的目录结构

2. **依赖注入**:
   - 使用构造函数注入依赖
   - 使用@Inject装饰器注入特殊依赖

### 代码风格

1. **TypeScript**:
   - 总是使用类型注解
   - 避免使用any
   - 使用接口定义数据结构

2. **异步处理**:
   - 使用async/await，不使用Promise.then()
   - 在service层处理异步操作
   - 使用try/catch处理异常

3. **注释**:
   - 为方法添加JSDoc注释，描述参数和返回值
   - 为复杂逻辑添加行内注释

## 最佳实践

1. **常量使用**:
   - 将魔法数字/字符串提取到常量文件
   - 按功能组织常量对象: `ARTICLE_APPROVAL_STATUS`, `REVIEW_MESSAGES`

2. **数据库操作**:
   - 使用TypeORM查询构建器
   - 重要操作使用事务保证原子性
   - 为查询添加适当的索引和连接

3. **错误处理**:
   - 使用Logger记录错误
   - 在controller层使用HttpException抛出HTTP错误
   - 提供清晰的错误消息

4. **缓存策略**:
   - 使用Redis缓存频繁访问的数据
   - 在数据变更时清除相关缓存
   - 使用常量定义缓存键和过期时间

5. **回调函数**:
   - 使用回调函数参数扩展方法功能
   - 保持函数单一职责

## 接口设计

1. **RESTful API**:
   - GET: 获取资源
   - POST: 创建资源
   - DELETE: 删除资源
   - 使用适当的HTTP状态码

2. **DTO验证**:
   - 使用class-validator进行输入验证
   - 定义清晰的DTO类型

3. **响应格式**:
   - 统一的响应结构，包含message和code
   - 使用常量定义响应消息

## 安全实践

1. **权限控制**:
   - 使用Role装饰器控制访问权限
   - 不同角色有不同的访问级别

2. **数据过滤**:
   - 在查询中添加适当的条件过滤数据
   - 审核状态控制数据可见性

## 异步操作

1. **异步任务**:
   - 使用异步方法处理耗时操作
   - 保持主流程响应迅速

2. **事务处理**:
   - 使用数据库事务保证一致性
   - 在事务失败时进行回滚